# Kivo placeholder icons

These neutral gray icons are temporary scaffold assets, not final Kivo branding.
They are generated from `kivo-placeholder.svg` with Tauri's official icon
generator:

```text
pnpm tauri icon src-tauri/icons/kivo-placeholder.svg --output src-tauri/icons
```

Replace source SVG and regenerate icon outputs before release branding review.
